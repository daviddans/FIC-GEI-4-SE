Explicación das optimizacións en ensamblador para reverse_int()
================================================================

1. Que fai a función C de referencia
--------------------------------------

A función inverte os 32 bits dun enteiro. Por cada bit fai:

    out = out << 1;     <- despraza a saida un bit á esquerda
    out |= in & 1;      <- copia o bit menos significativo de in a out
    in = in >> 1;       <- despraza a entrada un bit á dereita

Compilada con -Ofast, o compilador xera 7 instrucions por iteracion.
Con 32 iteracions e o custo das ramas: 312 ciclos medidos.


2. A optimizacion: carry flag (LSRS + ADCS)
---------------------------------------------

No ARM Cortex-M0+, a instrución LSRS (Logical Shift Right, Set flags)
non só despraza bits: tamén garda o bit que "sae" polo lado dereito nun
rexistro especial do procesador chamado carry flag. Este comportamento
está definido na especificación oficial ARMv6-M [1].

Logo, ADCS Rd, Rd calcula:  Rd = Rd + Rd + carry = (Rd << 1) | carry

Combinando as dúas instrucions:

    lsrs  in, in, #1      <- in >>= 1, o LSB orixinal vai ao carry
    adcs  out, out         <- out = (out << 1) | carry

Con 2 instrucions facemos o mesmo que con 5. Este uso do carry flag
para manipular bits sen ramas nin máscaras é unha técnica documentada
por Jens Bauer no blog oficial de ARM [3].

O bucle queda en 4 instrucions en vez de 7 -> 175 ciclos -> speedup 1.78x.


3. Por que non usamos RBIT?
----------------------------

Existe a instrución RBIT que inverte os 32 bits nunha soa operación,
pero só está dispoñible en ARMv7-M (Cortex-M3/M4/M7). O noso
Cortex-M0+ usa ARMv6-M, un subconxunto máis limitado.

A guía do Cortex-M0+ [2] lista todas as instrucions soportadas e RBIT
non aparece. Os propios mantedores de CMSIS de ARM confirman isto
explicitamente: "RBIT not available" en M0, M0+ e M23 [5].

Joseph Yiu, enxeñeiro de ARM, explica as diferenzas entre ARMv6-M e
ARMv7-M no seu libro de referencia [4], incluíndo as instrucions de
manipulación de bits que ARMv7-M engadiu respecto a ARMv6-M.


4. Implementacion inline vs arquivo .s
----------------------------------------

Inline ASM (en main.c):
  Código ensamblador embebido nunha función C usando __asm volatile.
  O compilador integrao co resto do código C.

ASM puro (reverse_asm.s):
  Función completa en ensamblador nun arquivo separado.
  Segue a convención AAPCS: argumento en r0, resultado en r0.
  Non precisa gardar rexistros (usa só r0-r2, que o chamador xa asume
  que poden cambiar), polo que non hai instrucions push/pop.

Ambas dan o mesmo resultado porque o algoritmo é idéntico.


5. Resultados
--------------

    Entrada: 0x12345678   Saída: 0x1E6A2C48

    | Implementación        | Ciclos | Speedup |
    |-----------------------|--------|---------|
    | C referencia (-Ofast) |   312  |  1.00x  |
    | ASM inline            |   175  |  1.78x  |
    | ASM puro (.s)         |   175  |  1.78x  |

    Medición: SysTick con reloxo do procesador (~20.97 MHz FLL).


6. Conclusión
--------------

A mellora vén de aproveitar o carry flag, algo que o compilador non fai
nin con -Ofast. É un exemplo de optimización que require coñecer a
arquitectura do procesador, como documenta Bauer en [3].


Referencias
-----------

[1] ARM Limited. ARMv6-M Architecture Reference Manual (ARM DDI 0419E).
    https://developer.arm.com/documentation/ddi0419/latest/
    Especificación formal das instrucions LSRS e ADCS e o carry flag.

[2] ARM Limited. Cortex-M0+ Devices Generic User Guide (ARM DUI 0662).
    https://developer.arm.com/documentation/dui0662/latest/
    Lista de instrucions do Cortex-M0+; confirma a ausencia de RBIT.

[3] Bauer, Jens. "Arm Cortex-M0 assembly programming tips and tricks."
    ARM Community Blog, 22 agosto 2016.
    https://developer.arm.com/community/arm-community-blogs/b/architectures-and-processors-blog/posts/arm-cortex-m0-assembly-programming-tips-and-tricks
    Documenta o uso de LSRS+ADCS para manipulación de bits sen ramas.

[4] Yiu, Joseph. The Definitive Guide to ARM Cortex-M0 and Cortex-M0+
    Processors, 2nd ed. Newnes/Elsevier, 2015. ISBN 978-0-12-803277-0.
    https://shop.elsevier.com/books/the-definitive-guide-to-arm-cortex-m0-and-cortex-m0-processors/yiu/978-0-12-803277-0
    Referencia completa do Cortex-M0+; diferenzas ARMv6-M vs ARMv7-M.

[5] ARM-software. "Using RBIT Instruction for bit reversal memory
    reordering." CMSIS_5 Issue #1278, GitHub, 2021.
    https://github.com/ARM-software/CMSIS_5/issues/1278
    Os mantedores de CMSIS confirman que RBIT non está dispoñible en
    M0, M0+ nin M23.
